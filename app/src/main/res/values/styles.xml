<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MainActivityTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
    </style>

    <style name="AppTheme.NoActionBar" >
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.NoActionBar.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.NoActionBar.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="editText_style">
        <item name="android:background">@drawable/edit_text_background</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="MyTheme" parent="android:style/Theme.Holo.Light">
        <item name="android:actionOverflowButtonStyle">@style/ThemeOverlay.MyTheme</item>
    </style>

    <!--ThemeOverlay.AppCompat.Light-->
    <style name="ThemeOverlay.MyTheme" parent="android:style/Widget.Holo.ActionButton.Overflow">
        <item name="android:src">@color/iconColor</item>
        <item name="android:textColorSecondary">@color/titleColor</item>
        <item name="android:actionOverflowButtonStyle">@color/titleColor</item>
        <item name="android:colorBackground">@color/titleColor</item>
        <item name="android:textColor">@color/fontColor</item>
    </style>

</resources>
